services:
  backend:
    build: ./backend
    ports: ["4000:4000"]
    volumes:
      - sqlite_data:/data
  frontend:
    build: ./frontend
    ports: ["3000:3000"]
    environment:
      VITE_API_URL: http://localhost:4000/api
    depends_on: [backend]
volumes:
  sqlite_data:
